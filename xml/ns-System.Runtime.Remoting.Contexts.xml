<Namespace Name="System.Runtime.Remoting.Contexts">
  <Docs>
    <summary><span data-ttu-id="a8a07-101">Der <see cref="N:System.Runtime.Remoting.Contexts" />-Namespace enthält Objekte, die die Kontexte aller darin befindlichen Objekte definieren.</span><span class="sxs-lookup"><span data-stu-id="a8a07-101">The <see cref="N:System.Runtime.Remoting.Contexts" /> namespace contains objects that define the contexts all objects reside within.</span></span> <span data-ttu-id="a8a07-102">Ein Kontext ist eine geordnete Folge von Eigenschaften, die eine Umgebung für die darin befindlichen Objekte definiert.</span><span class="sxs-lookup"><span data-stu-id="a8a07-102">A context is an ordered sequence of properties that defines an environment for the objects within it.</span></span> <span data-ttu-id="a8a07-103">Kontexte werden während der Aktivierung für Objekte erstellt, die so konfiguriert sind, dass bestimmte automatische Dienste für sie erforderlich sind, z. B. Synchronisierung, Transaktionen, JIT-Aktivierung (Just-In-Time), Sicherheit usw.</span><span class="sxs-lookup"><span data-stu-id="a8a07-103">Contexts are created during the activation process for objects that are configured to require certain automatic services such synchronization, transactions, just-in-time (JIT) activation, security, and so on.</span></span> <span data-ttu-id="a8a07-104">In einem Kontext können sich mehrere Objekte befinden.</span><span class="sxs-lookup"><span data-stu-id="a8a07-104">Multiple objects can live inside a context.</span></span></summary>
    <remarks>
      <format type="text/markdown"><![CDATA[  
  
## Remarks  
 <span data-ttu-id="a8a07-105">Wenn ein neues Objekt erstellt wird,.NET Framework sucht nach einem kompatiblen Kontext zu oder erstellt einen neuen Kontext für das Objekt.</span><span class="sxs-lookup"><span data-stu-id="a8a07-105">Whenever a new object is created, the.NET Framework finds a compatible context or creates a new context for the object.</span></span> <span data-ttu-id="a8a07-106">Nachdem ein Objekt in einem Kontext platziert ist, bleibt es dort Lebensdauer.</span><span class="sxs-lookup"><span data-stu-id="a8a07-106">After an object is placed in a context, it stays in it for life.</span></span> <span data-ttu-id="a8a07-107">Klassen, die an einen Kontext gebunden werden können, werden als kontextgebundene Klassen bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="a8a07-107">Classes that can be bound to a context are called context-bound classes.</span></span> <span data-ttu-id="a8a07-108">Wenn von einem anderen Kontext zugegriffen wird, werden diese kontextgebundenen Klassen direkt mit einem Proxy verwiesen.</span><span class="sxs-lookup"><span data-stu-id="a8a07-108">When accessed from another context, these context-bound classes are referenced directly using a proxy.</span></span> <span data-ttu-id="a8a07-109">Ein Aufruf von einem Objekt in einem Kontext auf ein Objekt in einem anderen Kontext geht über einen Kontextproxy und, die von der Richtlinie, die von den kombinierten Kontexteigenschaften implementiert beeinflusst werden.</span><span class="sxs-lookup"><span data-stu-id="a8a07-109">A call from an object in one context to an object in another context will go through a context proxy and be affected by the policy implemented by the combined context properties.</span></span>  
  
 ]]></format>
    </remarks>
  </Docs>
</Namespace>